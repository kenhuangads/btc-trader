name: daily-update

on:
  schedule:
    # 00:07 UTC（台北 08:07）＝ 日K收盤後產生當日訊號；
    # 其餘每 4 小時（4H K 收盤後）僅追蹤在途單狀態（訊號不變，冪等）
    - cron: "7 0,4,8,12,16,20 * * *"
  workflow_dispatch:
    inputs:
      bootstrap:
        description: "強制重跑回測（重建統計與權重）"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: daily
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run engine
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          ARGS=""
          if [ "${{ inputs.bootstrap }}" = "true" ]; then ARGS="--bootstrap"; fi
          python -m engine.run_daily $ARGS

      - name: Commit & push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A docs/data state history
          if git diff --cached --quiet; then
            echo "no changes"; exit 0
          fi
          git commit -m "chore(data): daily update $(date -u +%F)"
          for i in 1 2 3; do
            git push && exit 0
            git pull --rebase
          done
          exit 1
